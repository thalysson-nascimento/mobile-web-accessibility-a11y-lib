import { ManagerFocusScreenMobileDirective } from './manager-focus-screen-mobile.directive';

describe('Directive: ManagerFocusScreenMobile', () => {
  it('should create an instance', () => {
    const directive = new ManagerFocusScreenMobileDirective(
      HTMLElement.arguments,
      HTMLElement.arguments
    );
    expect(directive).toBeTruthy();
  });
});
